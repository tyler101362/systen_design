<!DOCTYPE html>
<html lang="zh">
  <head>
    <meta charset="UTF-8" />
    <title>分散式字典</title>
  </head>
  <body>
    <h1>📖 分散式字典</h1>

    <form action="/books/upload" method="POST" enctype="multipart/form-data">
      <input type="text" name="title" placeholder="書籍標題" required />
      <input type="text" name="author" placeholder="作者" required />
      <input type="file" name="file" required />
      <button type="submit">📤 上傳書籍</button>
    </form>

    <form action="/search" method="GET">
      <input type="text" name="query" placeholder="搜尋書籍" />
      <button type="submit">🔍 搜尋</button>
    </form>

    <h2>📚 書籍列表</h2>
    <ul>
      <% books.forEach(book => { %>
      <li>
        <a href="/books/<%= book._id %>"><%= book.title %></a> - <%= book.author
        %>
      </li>
      <% }) %>
    </ul>
  </body>
</html>
